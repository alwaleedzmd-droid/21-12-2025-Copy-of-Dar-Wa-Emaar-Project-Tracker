# تعليمات تثبيت مكتبات التصدير

## 🎯 المكتبات المطلوبة
تم إضافة ميزات تصدير متقدمة لـ:
- **لوحة الإحصائيات**: تصدير pdf مع صور عالية الجودة
- **قائمة المشاريع والأعمال**: تصدير Excel بصفحة واحدة مع تنسيق RTL

## ✅ خطوات التثبيت الفوري

### أسهل طريقة - تثبيت جميع المكتبات:
```bash
npm install
```

### أو تثبيت المكتبات المطلوبة فقط:
```bash
npm install jspdf html2canvas
```

## 📦 المكتبات المضافة
| المكتبة | الاستخدام |
|--------|----------|
| **jspdf** | تحويل الصفحات إلى PDF احترافي |
| **html2canvas** | التقاط الصفحات كصور عالية الجودة |
| **xlsx** ✅ | موجود مسبقاً - تصدير Excel |

## 🚀 الاستخدام
### في لوحة الإحصائيات:
1. زر **تصدير Excel** (أخضر) - تحميل ملف Excel بتقرير شامل
2. زر **تصدير PDF** (أحمر) - تحميل ملف PDF عالي الجودة

### في صفحة المشاريع:
- زر **تصدير المشاريع Excel** (أخضر) - جميع المشاريع في صفحة واحدة مع RTL

## ⚠️ استكشاف الأخطاء

### إذا ظهرت رسالة خطأ:
```
حدث خطأ أثناء تصدير PDF:
فشل في تحميل مكتبة jspdf
```

**الحل:**
```bash
# حذف المكتبات القديمة
rm -r node_modules package-lock.json

# تثبيت جميع المكتبات من جديد
npm install

# أو على Windows:
rmdir /s /q node_modules
del package-lock.json
npm install
```

### التحقق من التثبيت:
```bash
npm list jspdf html2canvas
```

## 🎨 ميزات التصدير

### Excel:
✅ جميع المشاريع + أعمالهم في صفحة واحدة
✅ تنسيق RTL (من اليمين لليسار)
✅ أعمدة منسقة تلقائياً
✅ إحصائيات لكل مشروع

### PDF:
✅ صور عالية الجودة (2x resolution)
✅ عرض RTL تلقائي
✅ رأسية احترافية
✅ دعم الصفحات المتعددة
- عدم وجود أخطاء في Console المتصفح
- استخدام متصفح حديث (Chrome, Firefox, Edge)

## محتوى ملف Excel المصدّر
1. **ملخص الإحصائيات**: الأرقام الرئيسية
2. **الطلبات الفنية**: تفاصيل حسب الحالة
3. **طلبات الإفراغات**: تفاصيل حسب الحالة
4. **توزيع الأعمال**: تفاصيل لكل مشروع
